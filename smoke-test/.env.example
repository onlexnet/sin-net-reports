# Environment variables for smoke tests
# Copy this file to .env and modify as needed

# Database port (default: 1433)
# Change if you have SQL Server already running locally
DB_PORT=1433

# Timeentries service port (default: 8080)
TIMEENTRIES_PORT=8080

# WebAPI service port (default: 8081)
WEBAPI_PORT=8081

# WebApp frontend port (default: 3000)
WEBAPP_PORT=3000

# Database credentials
DB_PASSWORD=YourStrong@Passw0rd
DB_USER=sa
