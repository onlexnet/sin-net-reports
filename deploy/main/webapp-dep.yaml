apiVersion: apps/v1
kind: Deployment
metadata:
  name: webapp-dep
  labels:
    app: webapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: webapp
  
  template:
    metadata:
      labels:
        app: webapp
    
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: nginx
          image: nginx:1.17
          imagePullPolicy: Always
          ports:
          - containerPort: 80
