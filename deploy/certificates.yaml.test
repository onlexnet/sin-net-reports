apiVersion: certmanager.k8s.io/v1alpha1
kind: Certificate
metadata:
  name: tls-secret
spec:
  secretName: tls-secret
  dnsNames:
  - posesor.net
  acme:
    config:
    - http01:
        ingressClass: nginx
      domains:
      - posesor.net
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer