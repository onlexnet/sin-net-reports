apiVersion: v2
name: sinnet-reports
description: Time reporting application for SinNet company

type: application

# This is the chart version. This version number should be incremented each time you make changes
# to the chart and its templates, including the app version.
# Versions are expected to follow Semantic Versioning (https://semver.org/)
version: 0.3.15

# Do not use such value as long as https://github.com/helm/helm/issues/8194 is not finished
# Ideal should be use version combined from tags of build blocks
appVersion: 1.0.0


