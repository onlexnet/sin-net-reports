interface Entity {
    projectId: ID!
    entityId: ID!
    entityVersion: Int!
}

type SomeEntity implements Entity {
    projectId: ID!
    entityId: ID!
    entityVersion: Int!
}

input MyEntity {
    projectId: ID!
    entityId: ID!
    entityVersion: Int!
}

