query FetchServices($period: String!, $customerName: String!) {
  Services {
    search (filter: {
      whenProvided: $period,
      forWhatCustomer: $customerName,
      onlyMine: false
    }) {
      items {
        whenProvided
        forWhatCustomer
      }
    }
  }
}